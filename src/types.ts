export interface Config {
  solFrenAPI?: {
    apiKey: string
  }
  solanaRPC?: {
    endpoint: string
  }
  wonkaAPI?: {
    endpoint: string
  }
  twitter?: {
    apiKey: string
  }
  cyberConnect?: {
    endpoint: string
  }
  simpleHash?: {
    key: string
  }
}
